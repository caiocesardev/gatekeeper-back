version: '3.8'

services:
  database:
    image: postgres:18.4-alpine
    container_name: gatekeeper_db
    restart: always
    environment:
      POSTGRES_DB: gatekeeper_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    networks:
      - gatekeeper_net

  mqtt-broker:
    image: eclipse-mosquitto:2.1.2-alpine
    container_name: gatekeeper_mqtt
    restart: always
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./mosquitto/config/mosquitto.conf:/mosquitto/config/mosquitto.conf
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log
    networks:
      - gatekeeper_net

#  back-end:
#    build: .
#    container_name: gatekeeper_backend
#    restart: always
#    ports:
#      - "8080:8080"
#    depends_on:
#      - database
#      - mqtt-broker
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://database:5432/gatekeeper_db
#      SPRING_DATASOURCE_USERNAME: admin
#      SPRING_DATASOURCE_PASSWORD: adminpassword
#      MQTT_BROKER_URL: tcp://mqtt-broker:1883
#    networks:
#      - gatekeeper_net

volumes:
  postgres_data:
  mosquitto_data:
  mosquitto_log:

networks:
  gatekeeper_net:
    driver: bridge